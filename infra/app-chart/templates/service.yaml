apiVersion: v1
kind: Service
metadata:
  name: { { include "hivebox.fullname" . } }
  labels: { { - include "hivebox.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - name: http
      port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
  selector: { { - include "hivebox.selectorLabels" . | nindent 4 } }
