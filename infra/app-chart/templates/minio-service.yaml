{{- if .Values.minio.deploy -}}
apiVersion: v1
kind: Service
metadata:
  name: minio
  labels:
    {{- include "hivebox.labels" . | nindent 4 }}
    app: minio
spec:
  selector:
    app: minio
  ports:
    - name: api
      port: 9000
      targetPort: 9000
      protocol: TCP
    - name: console
      port: 9001
      targetPort: 9001
      protocol: TCP
{{- end }}
